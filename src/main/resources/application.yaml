spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study_json_datatype
    username: root
    password: cr7super

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: WARN
    org:
      springframework:
        secuirty: DEBUG
      hibernate:
        sql: DEBUG
        type.descriptor.sql.BasicBinder: TRACE
    com:
      salah:
        ask: trace

